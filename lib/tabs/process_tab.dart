import 'package:flutter/material.dart';

class ProcessTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: <Widget>[
        Text("Status de processamento, no estilo do meus pedidos")
      ],
    );
  }
}